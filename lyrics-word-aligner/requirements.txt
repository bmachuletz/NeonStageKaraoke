fastapi==0.116.1
uvicorn[standard]==0.35.0
python-multipart==0.0.20
numpy>=2.1,<3
soundfile>=0.13,<1
git+https://github.com/huggingface/transformers.git
accelerate>=1.9,<2
sentencepiece>=0.2,<1
pyphen>=0.17,<1
# Pin to CUDA 12.x / cuDNN 9. Newer 1.27 wheels require CUDA 13.
onnxruntime-gpu==1.20.1
audio-separator[gpu]>=0.35,<1
# Optional singing-specific English fallback (vendored SOFA v1.0.3).
lightning>=2.2,<3
h5py>=3.11,<4
matplotlib>=3.9,<4
pandas>=2.2,<4
textgrid==1.6.1
tensorboard>=2.17,<3
tensorboardX>=2.6,<3
# easyaligner 0.3.3 is installed without dependencies in the Dockerfile because
# its published Torch <2.9 pin is narrower than the CUDA-12.8 stack validated
# by this service. These are the lightweight runtime dependencies used by its
# alignment/text modules; VAD dependencies are intentionally not loaded.
msgspec==0.21.1
rapidfuzz==3.14.5
nltk==3.10.0
# Independent Whisper timing/transcript fallback.  It is used as an acoustic
# vote when Qwen ASR returns an empty or severely incomplete music transcript.
stable-ts==2.19.1
